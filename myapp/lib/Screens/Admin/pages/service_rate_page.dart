import 'package:flutter/material.dart';

class ServiceRatePage extends StatelessWidget {
  const ServiceRatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Service + Rate Page"),
    );
  }
}